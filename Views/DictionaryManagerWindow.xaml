<Window x:Class="DoorMonitorSystem.Views.DictionaryManagerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="字典管理" Height="550" Width="720"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize"
        Background="#1E1E1E" Foreground="White">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="TitleText" Text="管理字典" FontSize="18" FontWeight="Bold" Foreground="#00CCFF" Margin="0,0,0,15"/>

        <DataGrid x:Name="MainGrid" Grid.Row="1" AutoGenerateColumns="False" 
                  CanUserAddRows="False" Background="#252525" Foreground="White"
                  BorderBrush="#33FFFFFF" BorderThickness="1"
                  RowBackground="#2D2D2D" AlternatingRowBackground="#353535">
            <DataGrid.Resources>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="#333"/>
                    <Setter Property="Foreground" Value="#AAA"/>
                    <Setter Property="Padding" Value="5"/>
                </Style>
            </DataGrid.Resources>
            <DataGrid.Columns>
                <!-- Columns will be added in code behind based on type -->
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,15,0,0">
            <Button Content="➕ 添加行" Click="Add_Click" Width="100" Height="32" 
                    Background="#2E7D32" Foreground="White" BorderThickness="0" Cursor="Hand" Margin="0,0,10,0"/>
            <Button Content="🗑 删除选中" Click="Delete_Click" Width="100" Height="32" 
                    Background="#C62828" Foreground="White" BorderThickness="0" Cursor="Hand"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="💾 保存并关闭" Click="Save_Click" Width="120" Height="32" 
                    Background="#007ACC" Foreground="White" BorderThickness="0" Cursor="Hand" Margin="0,0,10,0"/>
            <Button Content="取消" Click="Cancel_Click" Width="80" Height="32" 
                    Background="#444" Foreground="White" BorderThickness="0" Cursor="Hand"/>
        </StackPanel>
    </Grid>
</Window>
