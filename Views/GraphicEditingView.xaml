<UserControl x:Class="DoorMonitorSystem.Views.GraphicEditingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:DoorMonitorSystem.ViewModels"
             mc:Ignorable="d"
             
             Background="#0d1117"  Height="720"  Width="800">
    <UserControl.DataContext>
        <vm:GraphicEditingViewModel />
    </UserControl.DataContext>

    <UserControl.Resources>
        <!-- 深色主题按钮样式 -->
        <Style x:Key="DarkButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#2196F3"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                               CornerRadius="6"
                               Padding="{TemplateBinding Padding}">
                            <Border.Effect>
                                <DropShadowEffect Color="#2196F3" BlurRadius="8" ShadowDepth="0" Opacity="0.4"/>
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#1976D2"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#0D47A1"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 删除按钮样式 -->
        <Style x:Key="DeleteButtonStyle" TargetType="Button" BasedOn="{StaticResource DarkButtonStyle}">
            <Setter Property="Background" Value="#d32f2f"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#f44336"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <!-- 成功按钮样式 -->
        <Style x:Key="SuccessButtonStyle" TargetType="Button" BasedOn="{StaticResource DarkButtonStyle}">
            <Setter Property="Background" Value="#4CAF50"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#66BB6A"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- 深色文本框样式 -->
        <Style x:Key="DarkTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="#1a1f2e"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="CaretBrush" Value="White"/>
            <Setter Property="SelectionBrush" Value="#2196F3"/>
            <Setter Property="BorderBrush" Value="#2a5f8f"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="1"/>
            <Setter Property="FontSize" Value="17"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                               BorderBrush="{TemplateBinding BorderBrush}"
                               BorderThickness="{TemplateBinding BorderThickness}"
                               CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost"
                                        Margin="{TemplateBinding Padding}"
                                        Foreground="{TemplateBinding Foreground}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#2196F3"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="20"/>
            <ColumnDefinition Width="1.2*"/>
        </Grid.ColumnDefinitions>

        <!-- 左侧：导入和预览区域 -->
        <Border Grid.Column="0" Background="#161b22" CornerRadius="12" BorderBrush="#2a5f8f" BorderThickness="1">
            <Border.Effect>
                <DropShadowEffect Color="Black" BlurRadius="20" ShadowDepth="3" Opacity="0.5"/>
            </Border.Effect>

            <StackPanel Margin="20">
                <!-- 标题 -->
                <Border Background="#1e3a5f" CornerRadius="8" Padding="15,12" Margin="0,0,0,20">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="📁" FontSize="20" Margin="0,0,10,0"/>
                        <TextBlock Text="SVG 矢量图导入" FontSize="18" FontWeight="Bold" Foreground="White"/>
                    </StackPanel>
                </Border>

                <!-- 导入 SVG 按钮 -->
                <Button Content="📂 加载 SVG 文件"
                        Style="{StaticResource SuccessButtonStyle}"
                        Command="{Binding ImportSvgCommand}"
                        Height="50"
                        FontSize="16"
                        Margin="0,0,0,20"/>

                <!-- 图形名称输入 -->
                <TextBlock Text="图标标识:" Foreground="#a0b0c0" FontSize="13" Margin="0,0,0,8"/>
                <TextBox Style="{StaticResource DarkTextBoxStyle}"
                         Text="{Binding IconName, UpdateSourceTrigger=PropertyChanged}"
                         Height="45"
                         Margin="0,0,0,20"/>

                <!-- 保存按钮 -->
                <Button Content="💾 存入组件库"
                        Style="{StaticResource DarkButtonStyle}"
                        Command="{Binding SaveCommand}"
                        Height="45"
                        FontSize="15"
                        Margin="0,0,0,20"/>

                <!-- 图形预览 -->
                <TextBlock Text="效果预览:" Foreground="#a0b0c0" FontSize="13" Margin="0,0,0,8"/>
                <Border BorderBrush="#2a5f8f" BorderThickness="2" CornerRadius="8"
                        Background="#1a1f2e"  > 
                    <Viewbox  Grid.Row="1" Margin="5"  >
                        <ItemsControl  ItemsSource="{Binding PathItems}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <!-- 所有图形叠加在同一个Grid中 -->
                                    <Grid/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Path 
                                          Width="1024" Height="1024"
                                          Data="{Binding Data}"
                                          Fill="{Binding Fill}"
                                          Stroke="{Binding Stroke}"
                                          StrokeThickness="{Binding StrokeThickness}">
                                    </Path>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </Viewbox> 
                </Border>

                <!-- 提示信息 -->
                <TextBlock Text="💡 支持导入标准 SVG 矢量图形（兼容 Inkscape, Figma, Illustrator 等导出格式）"
                          Foreground="#7b8a9a" FontSize="11"
                          Margin="0,15,0,0" TextWrapping="Wrap"/>
            </StackPanel>
        </Border>

        <!-- 右侧：图形字典管理 -->
        <Border Grid.Column="2" Background="#161b22" CornerRadius="12" BorderBrush="#2a5f8f" BorderThickness="1">
            <Border.Effect>
                <DropShadowEffect Color="Black" BlurRadius="20" ShadowDepth="3" Opacity="0.5"/>
            </Border.Effect>

            <Grid Margin="20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- 标题 -->
                <Border Grid.Row="0" Background="#1e3a5f" CornerRadius="8" Padding="15,12" Margin="0,0,0,15">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="📚" FontSize="20" Margin="0,0,10,0"/>
                        <TextBlock Text="图形组件库" FontSize="18" FontWeight="Bold" Foreground="White"/>
                    </StackPanel>
                </Border>

                <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,15">
                    <Button Content="💾 同步至数据库"
                            Style="{StaticResource DarkButtonStyle}"
                            Command="{Binding ExportDictionaryCommand}"
                            Padding="15,8"
                            Margin="0,0,10,0"/>
                    <Button Content="� 从文件导入"
                            Style="{StaticResource DarkButtonStyle}"
                            Command="{Binding ImportDictionaryCommand}"
                            Padding="15,8"/>
                </StackPanel>

                <!-- 字典列表 -->
                <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{Binding GraphicGroups}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="#1a1f2e" CornerRadius="8"
                                       BorderBrush="#2a5f8f" BorderThickness="1"
                                       Margin="0,0,0,15" Padding="15">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>

                                        <!-- 图形名称 -->
                                        <TextBlock Grid.Row="0" Text="{Binding Name}"
                                                  FontWeight="Bold" FontSize="15"
                                                  Foreground="White"
                                                  Margin="0,0,0,10"/>

                                        <!-- 图形预览 -->
                                        <Border Grid.Row="1" BorderBrush="#3a4555" BorderThickness="1"
                                               CornerRadius="6" Background="White"
                                               Height="150" Margin="0,0,0,10">
                                            <Viewbox Stretch="Uniform" Margin="5">
                                                <ItemsControl ItemsSource="{Binding Items}">
                                                    <ItemsControl.ItemsPanel>
                                                        <ItemsPanelTemplate>
                                                            <!-- 使用 Grid 而不是 Canvas，与预览面板一致 -->
                                                            <Grid/>
                                                        </ItemsPanelTemplate>
                                                    </ItemsControl.ItemsPanel>
                                                    <ItemsControl.ItemTemplate>
                                                        <DataTemplate>
                                                            <Path Stroke="{Binding Stroke}"
                                                                 Fill="{Binding Fill}"
                                                                 StrokeThickness="{Binding StrokeThickness}"
                                                                 Data="{Binding Data}"/>
                                                        </DataTemplate>
                                                    </ItemsControl.ItemTemplate>
                                                </ItemsControl>
                                            </Viewbox>
                                        </Border>

                                        <!-- 删除按钮 -->
                                        <Button Grid.Row="2" Content="🗑️ 删除"
                                               Style="{StaticResource DeleteButtonStyle}"
                                               Command="{Binding DataContext.DeleteGraphicCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                               CommandParameter="{Binding Name}"
                                               HorizontalAlignment="Right"
                                               Padding="12,6"/>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Grid>
        </Border>
    </Grid>
</UserControl>
    
