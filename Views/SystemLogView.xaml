<UserControl x:Class="DoorMonitorSystem.Views.SystemLogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DoorMonitorSystem.Views"
             xmlns:uc="clr-namespace:DoorMonitorSystem.UControl"
             mc:Ignorable="d" 
           
             Background="Transparent">
    
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        
        <Style x:Key="CenterText" TargetType="TextBlock">
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Padding" Value="5,0"/>
        </Style>
        
        <Style x:Key="LeftText" TargetType="TextBlock">
            <Setter Property="HorizontalAlignment" Value="Left"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="Padding" Value="10,0"/>
        </Style>

        <Style x:Key="LeftHeaderStyle" TargetType="DataGridColumnHeader">
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Background" Value="#1e2430"/>
            <Setter Property="Foreground" Value="#a0b0c0"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="BorderBrush" Value="#2a3f5f"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 1. 顶部工具栏 -->
        <Border Grid.Row="0" Background="#1e2430" CornerRadius="5" Padding="10" Margin="0,0,0,10"
                BorderBrush="#2a3f5f" BorderThickness="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- 左侧筛选区 (使用 WrapPanel 避免窄屏下被截断) -->
                <WrapPanel Grid.Column="0" VerticalAlignment="Center">
                    <StackPanel Orientation="Horizontal" Margin="0,5,15,5">
                        <TextBlock Text="开始时间:" Foreground="#a0b0c0" VerticalAlignment="Center" Margin="0,0,5,0"/>
                        <uc:DateTimePicker SelectedDateTime="{Binding StartFullDateTime}" Width="210" />
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Margin="0,5,15,5">
                        <TextBlock Text="结束时间:" Foreground="#a0b0c0" VerticalAlignment="Center" Margin="0,0,5,0"/>
                        <uc:DateTimePicker SelectedDateTime="{Binding EndFullDateTime}" Width="210" />
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Margin="0,5,15,5"
                                Visibility="{Binding HasAnalogPoints, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <TextBlock Text="日志类型:" Foreground="#a0b0c0" VerticalAlignment="Center" Margin="0,0,5,0"/>
                        <ComboBox ItemsSource="{Binding LogTableTypes}" SelectedItem="{Binding SelectedTableType}" Width="110" Height="28" 
                                  VerticalContentAlignment="Center"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Margin="0,5,15,5">
                        <TextBlock Text="分组:" Foreground="#a0b0c0" VerticalAlignment="Center" Margin="0,0,5,0"/>
                        <ComboBox ItemsSource="{Binding Categories}" SelectedItem="{Binding SelectedCategory}" 
                                  Width="120" Height="28" 
                                  IsEditable="True" MaxDropDownHeight="300"
                                  IsTextSearchEnabled="True" TextSearch.TextPath="."
                                  IsTextSearchCaseSensitive="False"
                                  VirtualizingPanel.IsVirtualizing="True"
                                  VerticalContentAlignment="Center">
                        </ComboBox>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Margin="0,5,15,5">
                        <TextBlock Text="日志类型:" Foreground="#a0b0c0" VerticalAlignment="Center" Margin="0,0,5,0"/>
                        <ComboBox ItemsSource="{Binding LogLevelOptions}" SelectedItem="{Binding SelectedLogLevel}" Width="80" Height="28" 
                                  VerticalContentAlignment="Center"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Margin="0,5,15,5">
                        <TextBlock Text="关键字:" Foreground="#a0b0c0" VerticalAlignment="Center" Margin="0,0,5,0"/>
                        <Grid>
                            <TextBox x:Name="KeywordBox" Text="{Binding Keyword, UpdateSourceTrigger=PropertyChanged}" 
                                     Width="150" Height="28" VerticalContentAlignment="Center" 
                                     Background="#11FFFFFF" Foreground="White" BorderBrush="#44FFFFFF" Padding="5,0,25,0"/>
                            <Button HorizontalAlignment="Right" VerticalAlignment="Center" Width="20" Height="28"
                                    Margin="0,0,2,0" Background="Transparent" BorderThickness="0" Foreground="#88FFFFFF"
                                    FontSize="14" Cursor="Hand" Click="ClearKeyword_Click">
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Setter Property="Content" Value="×"/>
                                        <Setter Property="Visibility" Value="Visible"/>
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="Button">
                                                    <Border Background="Transparent">
                                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                    </Border>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                        <Style.Triggers>
                                            <!-- 当文本为空时隐藏按钮 -->
                                            <DataTrigger Binding="{Binding Text, ElementName=KeywordBox}" Value="">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Text, ElementName=KeywordBox}" Value="{x:Null}">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                            </DataTrigger>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Foreground" Value="White"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                            </Button>
                        </Grid>
                    </StackPanel>
                </WrapPanel>

                <!-- 右侧按钮区 -->
                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Bottom" Margin="0,0,0,5">
                    <Button Command="{Binding SearchCommand}" 
                            Style="{StaticResource ActionButton}" Background="#1976d2" BorderThickness="0" Width="90">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="🔍" Margin="0,0,5,0" VerticalAlignment="Center" FontSize="12"/>
                            <TextBlock Text="查询" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                    <Button Command="{Binding ResetCommand}" 
                            Style="{StaticResource ActionButton}" Background="#455a64" BorderThickness="0" Width="90">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="🔄" Margin="0,0,5,0" VerticalAlignment="Center" FontSize="12"/>
                            <TextBlock Text="重置" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                    <Button Command="{Binding ExportCommand}" 
                            Style="{StaticResource ActionButton}" Background="#2e7d32" BorderThickness="0" Width="90">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="📥" Margin="0,0,5,0" VerticalAlignment="Center" FontSize="12"/>
                            <TextBlock Text="导出" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <!-- 2. 数据表格 -->
        <DataGrid Grid.Row="1" ItemsSource="{Binding Logs}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True"
                  HeadersVisibility="Column" GridLinesVisibility="Horizontal"
                  CanUserSortColumns="False"
                  Background="#151921" BorderThickness="1" BorderBrush="#2a3f5f"
                  RowHeight="35" FontSize="14">
            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="#1e2430"/>
                    <Setter Property="Foreground" Value="#a0b0c0"/>
                    <Setter Property="Padding" Value="10,8"/>
                    <Setter Property="BorderThickness" Value="0,0,0,1"/>
                    <Setter Property="BorderBrush" Value="#2a3f5f"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                    <Setter Property="HorizontalContentAlignment" Value="Center"/>
                </Style>
            </DataGrid.ColumnHeaderStyle>
            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Foreground" Value="#e0e0e0"/>
                    <Style.Triggers>
                        <!-- 鼠标悬停高亮 -->
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#2a3245"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.RowStyle>
            
            <DataGrid.Columns>
                <DataGridTextColumn Header="序号" Binding="{Binding RowIndex}" Width="60" ElementStyle="{StaticResource CenterText}"/>
                <DataGridTextColumn Header="记录时间" Binding="{Binding LogTime, StringFormat='yyyy-MM-dd HH:mm:ss.fff'}" Width="190" ElementStyle="{StaticResource CenterText}"/>
                <DataGridTextColumn Header="日志分类" Binding="{Binding LogTypeDisplay}" Width="80" ElementStyle="{StaticResource CenterText}"/>
                <!-- Configured to show Category/UserName instead of Device/Address -->
                <DataGridTextColumn Header="业务分组" Binding="{Binding Category}" Width="100" ElementStyle="{StaticResource CenterText}"/>
                <DataGridTextColumn Header="变量地址" Binding="{Binding Address}" Width="120" ElementStyle="{StaticResource CenterText}"/>
                <DataGridTextColumn Header="用户" Binding="{Binding UserName}" Width="80" ElementStyle="{StaticResource CenterText}"/>
                <DataGridTextColumn Header="值" Binding="{Binding ValDisplay}" Width="80" ElementStyle="{StaticResource CenterText}"/>
                <DataGridTextColumn Header="日志内容" Binding="{Binding Message}" Width="*" HeaderStyle="{StaticResource LeftHeaderStyle}">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Left"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="Padding" Value="10,0"/>
                            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- 3. 底部翻页栏 -->
        <Border Grid.Row="2" Background="#1e2430" CornerRadius="0,0,5,5" 
                BorderBrush="#2a3f5f" BorderThickness="1,0,1,1" Padding="10,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <TextBlock Text="总记录数:" Foreground="#a0b0c0" VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding TotalCount}" Foreground="White" FontWeight="Bold" Margin="5,0,0,0" VerticalAlignment="Center"/>
                    <TextBlock Text="条" Foreground="#a0b0c0" VerticalAlignment="Center" Margin="2,0,0,0"/>
                </StackPanel>

                <StackPanel Grid.Column="2" Orientation="Horizontal">
                    <TextBlock Text="第" Foreground="#a0b0c0" VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding CurrentPage}" Foreground="White" FontWeight="Bold" Margin="5,0" VerticalAlignment="Center"/>
                    <TextBlock Text="/" Foreground="#a0b0c0" VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding TotalPages}" Foreground="White" Margin="5,0" VerticalAlignment="Center"/>
                    <TextBlock Text="页" Foreground="#a0b0c0" VerticalAlignment="Center" Margin="0,0,15,0"/>

                    <Button Content="首页" Command="{Binding FirstPageCommand}" 
                            Style="{StaticResource ActionButton}" Height="25" Width="60" Background="#37474f" Margin="0,0,5,0"/>
                    <Button Content="上一页" Command="{Binding PrevPageCommand}" 
                            Style="{StaticResource ActionButton}" Height="25" Width="70" Background="#37474f" Margin="0,0,5,0"/>
                    <Button Content="下一页" Command="{Binding NextPageCommand}" 
                            Style="{StaticResource ActionButton}" Height="25" Width="70" Background="#37474f" Margin="0,0,5,0"/>
                    <Button Content="尾页" Command="{Binding LastPageCommand}" 
                            Style="{StaticResource ActionButton}" Height="25" Width="60" Background="#37474f" Margin="0,0,15,0"/>

                    <TextBlock Text="跳转至" Foreground="#a0b0c0" VerticalAlignment="Center" Margin="0,0,5,0"/>
                    <TextBox Text="{Binding JumpPage, UpdateSourceTrigger=PropertyChanged}" 
                             Width="50" Height="25" VerticalContentAlignment="Center" HorizontalContentAlignment="Center"
                             Background="#11FFFFFF" Foreground="White" BorderBrush="#44FFFFFF"/>
                    <Button Content="GO" Command="{Binding GoToPageCommand}" 
                            Style="{StaticResource ActionButton}" Height="25" Width="40" Background="#1976d2" Margin="5,0,0,0"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- 加载遮罩 -->
        <Grid Grid.Row="1" Background="#88000000" Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="正在查询数据..." Foreground="White" FontSize="16" FontWeight="Bold"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
