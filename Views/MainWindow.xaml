<Window xmlns:UControl="clr-namespace:DoorMonitorSystem.UControl"  
        x:Class="DoorMonitorSystem.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DoorMonitorSystem.Views"    
        xmlns:dc="clr-namespace:DoorMonitorSystem.ViewModels" 
        mc:Ignorable="d"
        x:Name="MainWin"
        Title="MainWindow"   
         WindowState="Normal"
        WindowStyle="None"
        AllowsTransparency="True" 
        Left="{Binding Source={x:Static SystemParameters.WorkArea}, Path=Left}"
        Top="{Binding Source={x:Static SystemParameters.WorkArea}, Path=Top}"
        Width="{Binding Source={x:Static SystemParameters.WorkArea}, Path=Width}"
        Height="{Binding Source={x:Static SystemParameters.WorkArea}, Path=Height}" 
        WindowStartupLocation="CenterScreen"  >
    <Window.DataContext>
        <dc:MainWindowViewModel/>
    </Window.DataContext>

    <Window.Resources> 
        
    </Window.Resources>
    <Window.Background >
        <ImageBrush ImageSource="/Assets/Images/bg.png" TileMode="None"/>
    </Window.Background>

    <Grid  >
        <Grid.RowDefinitions>
            <RowDefinition Height="AUTO"/>
            <RowDefinition Height= "*"/>
        </Grid.RowDefinitions>
        <!--头部-->
        <StackPanel  Margin="0,20,0,0">
            <Grid>
                <Grid.ColumnDefinitions >
                    <ColumnDefinition Width ="2*"/>
                    <ColumnDefinition Width ="auto"/>
                    <ColumnDefinition Width ="2*"/>
                </Grid.ColumnDefinitions>
                <!-- 导航按钮示例 -->
                <StackPanel Orientation="Horizontal"  >
                    <Button Content="主界面" Style="{ StaticResource  ButtonNavigation}"    Command="{Binding NavigationCommand}" CommandParameter="{x:Type dc:MainViewModel}" />
                    <Button Content="点表配置" Style="{ StaticResource  ButtonNavigation}"      Command="{Binding NavigationCommand}" CommandParameter="{x:Type dc:DevvarlistViewModel}"  />
                    <Button Content="图形编辑" Style="{ StaticResource  ButtonNavigation}"  Command="{Binding NavigationCommand}" CommandParameter="{x:Type dc:GraphicEditingViewModel}" />
                    <Button Content="设备列表" Style="{ StaticResource  ButtonNavigation}"  Command="{Binding NavigationCommand}" CommandParameter="{x:Type dc:DeployViewModel}" />
                    <Button Content="测试" Style="{ StaticResource  ButtonNavigation}"  Command="{Binding NavigationCommand}" CommandParameter="{x:Type dc:ConfigureViewModel}" />
                </StackPanel>

                <Grid  Grid.Column="1"    >
                    <Label Content="上位机监视系统"  FontFamily="Microsoft YaHei" FontSize="35" FontWeight="Bold" FontStyle="Italic"   Foreground="White"
                          HorizontalAlignment="Center" VerticalAlignment="Top"  />
                    <Viewbox Height="100" >
                        <Path Data="M0 0 A8 8 0 0 0 0 10 L40 78 A10 10 0 0 0 45 80
                                    L405 80 A10 10 0 0 0 410 78 L445 10 A8 8 0 0 0 445 0"
                                     Stroke=" White" StrokeThickness="1"  Fill="Transparent" Stretch="Fill" />
                    </Viewbox>

                    <Label Content="{Binding Time}" FontFamily="Microsoft YaHei"   FontSize="22"   Foreground="White"
                          VerticalContentAlignment="Bottom"  HorizontalAlignment="Center"
                          VerticalAlignment="Bottom"   />
                </Grid>

                <StackPanel Grid.Column="2" HorizontalAlignment="Right"  Orientation="Horizontal"> 

                    <Button Content="登入"   Style="{ StaticResource  ButtonNavigation}"  Command="{Binding  DeployCommand}" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"  />

                    <Button  Content="退出"  Style="{ StaticResource  ButtonNavigation}"  Command="{Binding ExecuteCommand}"   />
                </StackPanel>

            </Grid>
        </StackPanel> 
        <!--窗口-->
        <Border  Grid.Row="1" BorderBrush="Black"   Padding="2,5"  >
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition />
                    <RowDefinition Height="200"/>
                </Grid.RowDefinitions>
                <!--导行切换-->
                <ContentControl Content="{Binding CurrentView}"/> 
            </Grid>
        </Border>
    </Grid>
</Window>
 
