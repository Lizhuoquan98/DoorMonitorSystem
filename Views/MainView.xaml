<UserControl
    x:Class="DoorMonitorSystem.Views.MainView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="clr-namespace:DoorMonitorSystem.Views"
    xmlns:vm="clr-namespace:DoorMonitorSystem.ViewModels"
    xmlns:models="clr-namespace:DoorMonitorSystem.Models.RunModels"
    xmlns:usc="clr-namespace:ControlLibrary;assembly=ControlLibrary"
    xmlns:conv="clr-namespace:DoorMonitorSystem.Assets.Converter"
    mc:Ignorable="d"
    Tag="主界面"
    UseLayoutRounding="True"
    TextOptions.TextFormattingMode="Display"
    TextOptions.TextRenderingMode="ClearType">



    <UserControl.Resources>
        <!-- 工业级门展示模板：恢复空间库 (ControlLibrary) 的 DoorControl 模型 -->
        <DataTemplate x:Key="DoorTemplate" DataType="{x:Type models:DoorModel}">
            <usc:DoorControl Width="54" Height="84" Margin="1,0"
                            HeaderText="{Binding DoorName}"
                            HeaderBackground="{Binding Visual.HeaderBackground}"
                            BottomBackground="{Binding Visual.BottomBackground}"
                            IconItems="{Binding Visual.IconItems}"
                            Command="{Binding DataContext.OpenDoorDetailCommand, RelativeSource={RelativeSource AncestorType=local:MainView}}"
                            CommandParameter="{Binding}"/>
        </DataTemplate>

        <!-- 门组模板：恢复 Viewbox 适配模型，确保全屏幕适配 -->
        <DataTemplate x:Key="DoorGroupTemplate" DataType="{x:Type models:DoorGroup}">
            <ItemsControl ItemsSource="{Binding Doors}" Margin="4,2">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Left"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <StaticResource ResourceKey="DoorTemplate"/>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </DataTemplate>

        <!-- 面板点位模板 (恢复空间库 BitControl) -->
        <DataTemplate x:Key="PanelBitTemplate" DataType="{x:Type models:PanelBitConfig}">
                <usc:BitControl Margin="2,1" 
                                Value="{Binding BitValue}" 
                                BITColor="{Binding ConfigColor}" 
                                Text="{Binding Description}" 
                                Foreground="White"
                                Cursor="Hand">
                    <usc:BitControl.InputBindings>
                        <MouseBinding MouseAction="LeftClick" Command="{Binding DataContext.OpenDoorDetailCommand, RelativeSource={RelativeSource AncestorType={x:Type local:MainView}}}" CommandParameter="{Binding}"/>
                    </usc:BitControl.InputBindings>
                </usc:BitControl>
        </DataTemplate>

        <!-- 优化后的面板模板（顶部标题） -->
        <DataTemplate x:Key="PanelTopTemplate" DataType="{x:Type models:PanelModel}">
            <Border MinWidth="120" Margin="8,5" CornerRadius="6"
                    Background="#1a1f2e" BorderBrush="#2a5f8f" BorderThickness="1">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- 顶部标题 -->
                    <Border Grid.Row="0" Background="#1e3a5f" CornerRadius="5,5,0,0" Padding="10,8">
                        <TextBlock Text="{Binding PanelName}"
                                  FontSize="16" FontWeight="Bold"
                                  Foreground="#FFCAB83E"
                                  HorizontalAlignment="Center"
                                  TextAlignment="Center"/>
                    </Border>

                    <!-- 点位内容 -->
                    <ItemsControl Grid.Row="1" ItemsSource="{Binding BitList}"
                                 ItemTemplate="{StaticResource PanelBitTemplate}"
                                 VerticalAlignment="Top"
                                 Padding="8">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <UniformGrid Rows="{Binding LayoutRows}"
                                            Columns="{Binding LayoutColumns}"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                    </ItemsControl>
                </Grid>
            </Border>
        </DataTemplate>

        <!-- 优化后的面板模板（底部标题） -->
        <DataTemplate x:Key="PanelBottomTemplate" DataType="{x:Type models:PanelModel}">
            <Border MinWidth="120" Margin="8,5" CornerRadius="6"
                    Background="#1a1f2e" BorderBrush="#2a5f8f" BorderThickness="1">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- 点位内容 (添加 VerticalAlignment="Top" 防止稀疏分布) -->
                    <ItemsControl Grid.Row="0" ItemsSource="{Binding BitList}"
                                 ItemTemplate="{StaticResource PanelBitTemplate}"
                                 VerticalAlignment="Top"
                                 Padding="8">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <UniformGrid Rows="{Binding LayoutRows}"
                                            Columns="{Binding LayoutColumns}"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                    </ItemsControl>

                    <!-- 底部标题 -->
                    <Border Grid.Row="1" Background="#1e3a5f" CornerRadius="0,0,5,5" Padding="10,8">
                        <TextBlock Text="{Binding PanelName}"
                                  FontSize="16" FontWeight="Bold"
                                  Foreground="#FFCAB83E"
                                  HorizontalAlignment="Center"
                                  TextAlignment="Center"/>
                    </Border>
                </Grid>
            </Border>
        </DataTemplate>

        <!-- 面板组模板（根据面板的 TitlePosition 自动选择模板） -->
        <DataTemplate x:Key="PanelGroupTemplate" DataType="{x:Type models:PanelGroup}">
            <ItemsControl ItemsSource="{Binding Panels}" Margin="0,2">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <ContentControl Content="{Binding}">
                            <ContentControl.Style>
                                <Style TargetType="ContentControl">
                                    <Style.Triggers>
                                        <!-- TitlePosition = 1 使用顶部标题模板 -->
                                        <DataTrigger Binding="{Binding TitlePosition}" Value="1">
                                            <Setter Property="ContentTemplate" Value="{StaticResource PanelTopTemplate}"/>
                                        </DataTrigger>
                                        <!-- TitlePosition = 2 使用底部标题模板 -->
                                        <DataTrigger Binding="{Binding TitlePosition}" Value="2">
                                            <Setter Property="ContentTemplate" Value="{StaticResource PanelBottomTemplate}"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                    <!-- 默认使用底部标题模板 -->
                                    <Setter Property="ContentTemplate" Value="{StaticResource PanelBottomTemplate}"/>
                                </Style>
                            </ContentControl.Style>
                        </ContentControl>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </DataTemplate>

        <!-- 站台显示项模板选择器（自动选择门组或面板组模板） -->
        <local:StationItemTemplateSelector x:Key="StationItemTemplateSelector"
            DoorGroupTemplate="{StaticResource DoorGroupTemplate}"
            PanelGroupTemplate="{StaticResource PanelGroupTemplate}"/>

    </UserControl.Resources>

    <Grid>
        <!-- 主内容区 - 站台列表（全屏显示） -->
        <!-- 优化：修复虚拟化逻辑。将 ScrollViewer 移入 ItemsControl 内部，确保 VirtualizingStackPanel 生效 -->
        <ItemsControl ItemsSource="{Binding Stations}"
                      VirtualizingPanel.IsVirtualizing="True"
                      VirtualizingPanel.VirtualizationMode="Recycling"
                      VirtualizingPanel.CacheLengthUnit="Item"
                      VirtualizingPanel.CacheLength="2,2"
                      ScrollViewer.CanContentScroll="True"
                      ScrollViewer.VerticalScrollBarVisibility="Auto">
            <ItemsControl.Template>
                <ControlTemplate TargetType="ItemsControl">
                    <ScrollViewer Focusable="False" Padding="{TemplateBinding Padding}">
                        <ItemsPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                    </ScrollViewer>
                </ControlTemplate>
            </ItemsControl.Template>
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <VirtualizingStackPanel Orientation="Vertical"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>

                <!-- 单个站台模板（极简容器版） -->
                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="{x:Type vm:StationViewModel}">
                        <!-- 极简站台卡片 - 只作为内容容器 -->
                        <Border BorderThickness="0" CornerRadius="10"
                                Margin="0,8" Padding="20"
                                Background="#1a1f2e">
                            <!-- 站台内容（门组和面板组混合显示） -->
                            <ItemsControl ItemsSource="{Binding DisplayItems}"
                                         ItemTemplateSelector="{StaticResource StationItemTemplateSelector}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <VirtualizingStackPanel Orientation="Vertical" HorizontalAlignment="Center"/>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                            </ItemsControl>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
        </ItemsControl>

        <!-- 门详情弹窗（优化版：Grid Overlay 替代 Popup） -->
        <!-- 优化：全屏遮罩背景，提升视觉焦点；点击背景可关闭弹窗 -->
        <Grid x:Name="OverlayContainer"
              Panel.ZIndex="1000"
              MouseLeftButtonDown="OverlayBackground_MouseLeftButtonDown">
            <Grid.Style>
                <Style TargetType="Grid">
                    <Setter Property="Background" Value="#99000000"/> <!-- 半透明暗化背景 -->
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsPopupOpen}" Value="True">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Grid.Style>

            <!-- 拖动快照层（仅拖动时可见） -->
            <Image x:Name="PopupDragSnapshot"
                   Panel.ZIndex="1001"
                   Visibility="Collapsed"
                   IsHitTestVisible="False"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   Stretch="Fill"/>

            <!-- 主弹窗边框 -->
            <Border x:Name="PopupBorder" CornerRadius="12" MaxWidth="1200" MaxHeight="650"
                   HorizontalAlignment="Center" VerticalAlignment="Center"
                   Background="#1a1f2e" BorderBrush="#3a6f9f" BorderThickness="2">
                <Border.RenderTransform>
                    <TranslateTransform x:Name="PopupTranslateTransform"/>
                </Border.RenderTransform>
                <!-- 优化：添加阴影效果，增强层级感 -->
                <Border.Effect>
                    <DropShadowEffect BlurRadius="30" ShadowDepth="0" Opacity="0.7" Color="Black"/>
                </Border.Effect>

                
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- 标题栏（可拖动区域） -->
                        <Border Grid.Row="0" CornerRadius="15,15,0,0" 
                                MouseLeftButtonDown="Header_MouseLeftButtonDown"
                                MouseMove="Header_MouseMove"
                                MouseLeftButtonUp="Header_MouseLeftButtonUp"
                                Cursor="SizeAll"
                                Padding="20,15">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                    <GradientStop Color="#1e3a5f" Offset="0"/>
                                    <GradientStop Color="#2a5f8f" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.Background>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <!-- 弹窗头部门预览 (恢复空间库 DoorControl) -->
                                <usc:DoorControl Grid.Column="0" Width="40" Height="60" Margin="0,0,15,0"
                                                HeaderText="{Binding SelectedDoor.DoorName}"
                                                HeaderBackground="{Binding SelectedDoor.Visual.HeaderBackground}"
                                                BottomBackground="{Binding SelectedDoor.Visual.BottomBackground}"
                                                IconItems="{Binding SelectedDoor.Visual.IconItems}"/>

                                <!-- 标题文字 -->
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding PopupTitle}"
                                              FontSize="16" FontWeight="Bold"
                                              Foreground="White"/>
                                    <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                        <TextBlock Text="💡" FontSize="11" Margin="0,0,5,0"/>
                                        <TextBlock Text="按 ESC 键或右上角按钮关闭"
                                                  FontSize="11"
                                                  Foreground="#a0b0c0"/>
                                    </StackPanel>
                                </StackPanel>

                                <!-- 关闭按钮 -->
                                <Button Grid.Column="2"
                                        Margin="10,0,0,0"
                                       Command="{Binding ClosePopupCommand}"
                                       Width="38" Height="38"
                                       Background="#d32f2f"
                                       BorderThickness="0"
                                       Cursor="Hand">
                                    <Button.Template>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}"
                                                   CornerRadius="8">
                                                <TextBlock Text="✕"
                                                          FontSize="18" FontWeight="Bold"
                                                          Foreground="White"
                                                          HorizontalAlignment="Center"
                                                          VerticalAlignment="Center"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="#f44336"/>
                                                </Trigger>
                                                <Trigger Property="IsPressed" Value="True">
                                                    <Setter Property="Background" Value="#c62828"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Button.Template>
                                </Button>
                            </Grid>
                        </Border>

                        <!-- 点位详情内容区 -->
                        <ScrollViewer Grid.Row="1"
                                     VerticalScrollBarVisibility="Auto"
                                     HorizontalScrollBarVisibility="Disabled"
                                     MaxHeight="480"
                                     Margin="20,20,20,15"
                                     Background="#141824">
                            <!-- 使用 CategoryGroups 动态显示所有分类 -->
                            <ItemsControl ItemsSource="{Binding CategoryGroups}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center"/>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <!-- 动态分类卡片 -->
                                        <Border Margin="8"
                                               CornerRadius="10"
                                               Background="#1a1a2e"
                                               BorderBrush="{Binding Category.BackgroundBrush}"
                                               BorderThickness="2">
                                            <StackPanel>
                                                <!-- 动态分类标题 -->
                                                <Border CornerRadius="8,8,0,0"
                                                       Background="{Binding Category.BackgroundBrush}"
                                                       Padding="15,10">
                                                    <StackPanel Orientation="Horizontal">
                                                        <!-- 动态图标 -->
                                                        <TextBlock Text="{Binding Category.Icon}"
                                                                  FontSize="16"
                                                                  Margin="0,0,8,0"/>
                                                        <!-- 动态分类名称 -->
                                                        <TextBlock Text="{Binding Category.Name}"
                                                                  FontSize="16"
                                                                  FontWeight="Bold"
                                                                  Foreground="{Binding Category.ForegroundBrush}"/>
                                                        <!-- 激活数量徽章 -->
                                                        <Border Background="{Binding Category.ForegroundBrush}"
                                                               CornerRadius="10"
                                                               Padding="8,2"
                                                               Margin="8,0,0,0">
                                                            <Border.Style>
                                                                <Style TargetType="Border">
                                                                    <Setter Property="Visibility" Value="Visible"/>
                                                                    <Style.Triggers>
                                                                        <DataTrigger Binding="{Binding ActiveCount}" Value="0">
                                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                                        </DataTrigger>
                                                                    </Style.Triggers>
                                                                </Style>
                                                            </Border.Style>
                                                            <TextBlock Text="{Binding ActiveCount}"
                                                                      FontSize="12"
                                                                      FontWeight="Bold"
                                                                      Foreground="{Binding Category.BackgroundBrush}"/>
                                                        </Border>
                                                    </StackPanel>
                                                </Border>

                                                <!-- 点位列表 -->
                                                <ItemsControl ItemsSource="{Binding Bits}"
                                                              VerticalAlignment="Top"
                                                              Padding="10">
                                                    <ItemsControl.ItemsPanel>
                                                        <ItemsPanelTemplate>
                                                            <UniformGrid Rows="{Binding Category.LayoutRows}"
                                                                        Columns="{Binding Category.LayoutColumns}"/>
                                                        </ItemsPanelTemplate>
                                                    </ItemsControl.ItemsPanel>
                                                    <ItemsControl.ItemTemplate>
                                                        <DataTemplate>
                                                            <usc:BitControl Margin="1,2" Padding="4,2" MinHeight="26"
                                                                           Background="#FF1E2430" 
                                                                           BorderBrush="#33FFFFFF" 
                                                                           BorderThickness="1"
                                                                           Value="{Binding BitValue}"
                                                                           BITColor="{Binding ConfigColor}"
                                                                           Text="{Binding Description}"
                                                                           FontSize="14"
                                                                           Foreground="White"/>
                                                        </DataTemplate>
                                                    </ItemsControl.ItemTemplate>
                                                </ItemsControl>
                                            </StackPanel>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>

                        <!-- 底部提示 -->
                        <Border Grid.Row="2"
                               Background="#0d1117"
                               CornerRadius="0,0,15,15"
                               Padding="20,10"
                               BorderBrush="#2a5f8f"
                               BorderThickness="0,1,0,0">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                                <TextBlock Text="ℹ️" FontSize="11" Margin="0,0,6,0"/>
                                <TextBlock Text="按 ESC 键快速关闭"
                                          FontSize="11"
                                          Foreground="#7b8a9a"/>
                            </StackPanel>
                        </Border>
                    </Grid>
            </Border>
        </Grid>
        
    </Grid>
</UserControl>
