<UserControl
    x:Class="DoorMonitorSystem.Views.MainView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="clr-namespace:DoorMonitorSystem.Views"
    xmlns:vm="clr-namespace:DoorMonitorSystem.ViewModels"
    xmlns:models="clr-namespace:DoorMonitorSystem.Models.RunModels"
    xmlns:usc="clr-namespace:ControlLibrary;assembly=ControlLibrary"
    xmlns:conv="clr-namespace:DoorMonitorSystem.Assets.Converter"
    mc:Ignorable="d"
    Tag="主界面">

    <UserControl.DataContext>
        <vm:MainViewModel />
    </UserControl.DataContext>

    <UserControl.Resources>

        <!-- 转换器 -->
        <conv:CountToVisibilityConverter x:Key="CountToVisibilityConverter"/>

        <!-- 门控件模板（使用现有的 DoorControl） -->
        <DataTemplate x:Key="DoorTemplate" DataType="{x:Type models:DoorModel}">
            <usc:DoorControl
                HeaderText="{Binding Visual.HeaderText}"
                HeaderBackground="{Binding Visual.HeaderBackground}"
                BottomBackground="{Binding Visual.BottomBackground}"
                IconItems="{Binding Visual.Icons}"
                Command="{Binding DataContext.OpenDoorDetailCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                CommandParameter="{Binding}"
                Width="50" Height="80"
                Margin="2,0"
                FontSize="14"/>
        </DataTemplate>

        <!-- 门组模板 -->
        <DataTemplate x:Key="DoorGroupTemplate" DataType="{x:Type models:DoorGroup}">
            <ItemsControl ItemsSource="{Binding Doors}" Margin="0,2">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal" HorizontalAlignment="Center"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <StaticResource ResourceKey="DoorTemplate"/>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </DataTemplate>

        <!-- 面板点位模板 -->
        <DataTemplate x:Key="PanelBitTemplate" DataType="{x:Type models:PanelBitConfig}">
            <usc:BitControl Text="{Binding Description}"
                           BITColor="{Binding DisplayBrush}"
                           Value="{Binding BitValue}"
                           FontSize="15" Height="25"
                           Margin="2,1"
                           HorizontalAlignment="Stretch"/>
        </DataTemplate>

        <!-- 面板模板（顶部标题） -->
        <DataTemplate x:Key="PanelTopTemplate" DataType="{x:Type models:PanelModel}">
            <GroupBox MinWidth="100" Margin="5,0">
                <GroupBox.Header>
                    <Label Content="{Binding PanelName}"
                           FontSize="20" Background="Black"
                           Foreground="#FFCAB83E" Padding="5,1"/>
                </GroupBox.Header>
                <ItemsControl ItemsSource="{Binding BitList}"
                             ItemTemplate="{StaticResource PanelBitTemplate}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <UniformGrid Rows="{Binding LayoutRows}"
                                        Columns="{Binding LayoutColumns}"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                </ItemsControl>
            </GroupBox>
        </DataTemplate>

        <!-- 面板模板（底部标题） -->
        <DataTemplate x:Key="PanelBottomTemplate" DataType="{x:Type models:PanelModel}">
            <GroupBox MinWidth="100" Margin="5,0">
                <GroupBox.Header>
                    <Label Content="{Binding PanelName}"
                           FontSize="20" Background="Black"
                           Foreground="#FFCAB83E" Padding="5,1"/>
                </GroupBox.Header>
                <ItemsControl ItemsSource="{Binding BitList}"
                             ItemTemplate="{StaticResource PanelBitTemplate}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <UniformGrid Rows="{Binding LayoutRows}"
                                        Columns="{Binding LayoutColumns}"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                </ItemsControl>
            </GroupBox>
        </DataTemplate>

        <!-- 面板组模板 -->
        <DataTemplate x:Key="PanelGroupTemplate" DataType="{x:Type models:PanelGroup}">
            <ItemsControl ItemsSource="{Binding Panels}"  Margin="0,2">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <StaticResource ResourceKey="PanelBottomTemplate"/>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </DataTemplate>

        <!-- 站台显示项模板选择器（自动选择门组或面板组模板） -->
        <local:StationItemTemplateSelector x:Key="StationItemTemplateSelector"
            DoorGroupTemplate="{StaticResource DoorGroupTemplate}"
            PanelGroupTemplate="{StaticResource PanelGroupTemplate}"/>

    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 顶部分隔线 -->
        <Border Height="50" BorderThickness="0,0,0,1" BorderBrush="Black" Margin="0,0,0,5"/>

        <!-- 主内容区 - 站台列表 -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="30,0">
            <ItemsControl ItemsSource="{Binding Stations}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Vertical"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <!-- 单个站台模板 -->
                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="{x:Type vm:StationViewModel}">
                        <Border BorderBrush="Gray" BorderThickness="1"
                                CornerRadius="5" Margin="0,10" Padding="10">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>

                                <!-- 站台标题 -->
                                <TextBlock Grid.Row="0"
                                          Text="{Binding Station.StationName}"
                                          FontSize="24" FontWeight="Bold"
                                          Foreground="White"
                                          HorizontalAlignment="Center"
                                          Margin="0,0,0,10"/>

                                <!-- 站台内容（门组和面板组混合显示） -->
                                <ItemsControl Grid.Row="1"
                                             ItemsSource="{Binding DisplayItems}"
                                             ItemTemplateSelector="{StaticResource StationItemTemplateSelector}">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <StackPanel Orientation="Vertical" HorizontalAlignment="Center"/>
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                </ItemsControl>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- 门详情弹窗 -->
        <Popup x:Name="popup"
               IsOpen="{Binding IsPopupOpen}"
               Placement="Center"
               AllowsTransparency="True"
               StaysOpen="True"
               Opened="Popup_Opened">
            <!-- 外层阴影边框 -->
            <Border Background="Transparent" Padding="15"
                    x:Name="PopupContent"
                    TextOptions.TextFormattingMode="Display"
                    TextOptions.TextRenderingMode="ClearType"
                    RenderOptions.ClearTypeHint="Enabled">
                <Border.Effect>
                    <DropShadowEffect Color="Black" BlurRadius="20" ShadowDepth="0" Opacity="0.5"/>
                </Border.Effect>

                <!-- 主弹窗边框 -->
                <Border CornerRadius="12" MaxWidth="800" MaxHeight="600"   Background="#1e2838">
                   

                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- 标题栏（可拖动） -->
                        <Border Grid.Row="0" CornerRadius="12,12,0,0"
                                Background="#1e3a5f" Padding="15,12"
                                MouseLeftButtonDown="Label_MouseLeftButtonDown"
                                MouseMove="Label_MouseMove"
                                MouseLeftButtonUp="Label_MouseLeftButtonUp"
                                Cursor="Hand">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <!-- 图标 -->
                                <Border Grid.Column="0"
                                       Width="32" Height="32"
                                       CornerRadius="6"
                                       Background="#2a5a8f"
                                       Margin="0,0,12,0">
                                    <TextBlock Text="🚪" FontSize="18"
                                              HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                                </Border>

                                <!-- 标题文字 -->
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding PopupTitle}"
                                              FontSize="16" FontWeight="Bold"
                                              Foreground="White"/>
                                    <TextBlock Text="点击标题栏可拖动窗口"
                                              FontSize="10"
                                              Foreground="#8899aa"
                                              Margin="0,2,0,0"/>
                                </StackPanel>

                                <!-- 关闭按钮 -->
                                <Button Grid.Column="2"
                                       Command="{Binding ClosePopupCommand}"
                                       Click="Close_Click"
                                       Width="32" Height="32"
                                       Background="#d32f2f"
                                       BorderThickness="0"
                                       Cursor="Hand">
                                    <Button.Template>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}"
                                                   CornerRadius="6">
                                                <TextBlock Text="✕"
                                                          FontSize="16" FontWeight="Bold"
                                                          Foreground="White"
                                                          HorizontalAlignment="Center"
                                                          VerticalAlignment="Center"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="#f44336"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Button.Template>
                                </Button>
                            </Grid>
                        </Border>

                        <!-- 点位详情内容区 -->
                        <ScrollViewer Grid.Row="1"
                                     VerticalScrollBarVisibility="Auto"
                                     HorizontalScrollBarVisibility="Disabled"
                                     MaxHeight="450"
                                     Margin="15,15,15,10">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">

                                <!-- 告警点位分组 -->
                                <Border Margin="5"  
                                       CornerRadius="8"
                                       Background="#1a1a2e"
                                       BorderBrush="#d32f2f"
                                       BorderThickness="2"
                                       Visibility="{Binding AlarmBits.Count, Converter={StaticResource CountToVisibilityConverter}}">
                                    <Border.Effect>
                                        <DropShadowEffect Color="#d32f2f" BlurRadius="8" ShadowDepth="0" Opacity="0.3"/>
                                    </Border.Effect>

                                    <StackPanel  >
                                        <!-- 告警标题 -->
                                        <Border Background="#d32f2f" CornerRadius="6,6,0,0" Padding="12,8">
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="⚠" FontSize="16" Margin="0,0,8,0"/>
                                                <TextBlock Text="告警" FontSize="16" FontWeight="Bold" Foreground="White"/>
                                                <!-- 数量徽章：显示激活的告警数量 -->
                                                <Border Background="#ffebee" CornerRadius="10"
                                                       Padding="8,2" Margin="8,0,0,0"
                                                       Visibility="{Binding ActiveAlarmCount, Converter={StaticResource CountToVisibilityConverter}}">
                                                    <TextBlock Text="{Binding ActiveAlarmCount}"
                                                              FontSize="12" FontWeight="Bold"
                                                              Foreground="#d32f2f"/>
                                                </Border>
                                            </StackPanel>
                                        </Border>

                                        <!-- 告警点位列表 -->
                                        <ItemsControl ItemsSource="{Binding AlarmBits}"  
                                                     MaxWidth="320" Padding="10">
                                            <ItemsControl.ItemsPanel>
                                                <ItemsPanelTemplate>
                                                    <WrapPanel   Orientation="Vertical" />
                                                </ItemsPanelTemplate>
                                            </ItemsControl.ItemsPanel>
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Border CornerRadius="6"
                                                           Background="#1e2838"
                                                           BorderBrush="#3a4555"
                                                           BorderThickness="1"
                                                           Margin="3,2"
                                                           Padding="8,4"
                                                           SnapsToDevicePixels="True"
                                                           UseLayoutRounding="True">
                                                        <Border.Effect>
                                                            <DropShadowEffect Color="Black" BlurRadius="4" ShadowDepth="1" Opacity="0.3"/>
                                                        </Border.Effect>
                                                        <usc:BitControl Text="{Binding Description}"
                                                                      BITColor="{Binding IndicatorBrush}"
                                                                      Value="{Binding BitValue}"
                                                                      FontSize="15" Height="28"
                                                                      MinWidth="120" MaxWidth="200"
                                                                      Foreground="White"/>
                                                    </Border>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </StackPanel>
                                </Border>

                                <!-- 状态点位分组 -->
                                <Border Margin="5"  
                                       CornerRadius="8"
                                       Background="#1a1a2e"
                                       BorderBrush="#1976d2"
                                       BorderThickness="2"
                                       Visibility="{Binding StatusBits.Count, Converter={StaticResource CountToVisibilityConverter}}">
                                    <Border.Effect>
                                        <DropShadowEffect Color="#1976d2" BlurRadius="8" ShadowDepth="0" Opacity="0.3"/>
                                    </Border.Effect>

                                    <StackPanel>
                                        <!-- 状态标题 -->
                                        <Border Background="#1976d2" CornerRadius="6,6,0,0" Padding="12,8">
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="ℹ" FontSize="16" Margin="0,0,8,0"/>
                                                <TextBlock Text="状态" FontSize="16" FontWeight="Bold" Foreground="White"/>
                                                <!-- 数量徽章：显示激活的状态数量 -->
                                                <Border Background="#e3f2fd" CornerRadius="10"
                                                       Padding="8,2" Margin="8,0,0,0"
                                                       Visibility="{Binding ActiveStatusCount, Converter={StaticResource CountToVisibilityConverter}}">
                                                    <TextBlock Text="{Binding ActiveStatusCount}"
                                                              FontSize="12" FontWeight="Bold"
                                                              Foreground="#1976d2"/>
                                                </Border>
                                            </StackPanel>
                                        </Border>

                                        <!-- 状态点位列表 -->
                                        <ItemsControl ItemsSource="{Binding StatusBits}"
                                                     MaxWidth="320" Padding="10">
                                            <ItemsControl.ItemsPanel>
                                                <ItemsPanelTemplate>
                                                    <WrapPanel Orientation="Vertical" MaxHeight="400"/>
                                                </ItemsPanelTemplate>
                                            </ItemsControl.ItemsPanel>
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Border CornerRadius="6"
                                                           Background="#1e2838"
                                                           BorderBrush="#3a4555"
                                                           BorderThickness="1"
                                                           Margin="3,2"
                                                           Padding="8,4"
                                                           SnapsToDevicePixels="True"
                                                           UseLayoutRounding="True">
                                                        <Border.Effect>
                                                            <DropShadowEffect Color="Black" BlurRadius="4" ShadowDepth="1" Opacity="0.3"/>
                                                        </Border.Effect>
                                                        <usc:BitControl Text="{Binding Description}"
                                                                      BITColor="{Binding IndicatorBrush}"
                                                                      Value="{Binding BitValue}"
                                                                      FontSize="15" Height="28"
                                                                      MinWidth="160" MaxWidth="300"
                                                                      Foreground="White"/>
                                                    </Border>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </StackPanel>
                                </Border>

                            </StackPanel>
                        </ScrollViewer>

                        <!-- 底部提示 -->
                        <Border Grid.Row="2"
                               Background="#16202e"
                               CornerRadius="0,0,12,12"
                               Padding="15,8">
                            <TextBlock Text="按 ESC 键或点击关闭按钮关闭窗口"
                                      FontSize="11"
                                      Foreground="#6b7a90"
                                      HorizontalAlignment="Center"/>
                        </Border>
                    </Grid>
                </Border>
            </Border>
        </Popup>
    </Grid>
</UserControl>
