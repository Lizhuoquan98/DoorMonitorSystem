<UserControl x:Class="DoorMonitorSystem.UControl.DateTimePicker"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DoorMonitorSystem.UControl"
             mc:Ignorable="d" 
             Focusable="True">

    <UserControl.Resources>
        <!-- 下拉按钮图标 -->
        <ControlTemplate x:Key="DropdownIconTemplate" TargetType="ContentControl">
            <Path Data="M 0 0 L 5 5 L 10 0 Z" Fill="{TemplateBinding Foreground}" VerticalAlignment="Center" HorizontalAlignment="Center"/>
        </ControlTemplate>

        <!-- 极致对比度 ComboBox 样式 -->
        <Style x:Key="TimeSelectorComboBox" TargetType="ComboBox">
            <Setter Property="Width" Value="50"/>
            <Setter Property="Height" Value="24"/>
            <Setter Property="Margin" Value="2,0"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Background" Value="#1A1C23"/>
            <Setter Property="BorderBrush" Value="#00E5FF"/>
            <Setter Property="BorderThickness" Value="1.5"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton Name="ToggleButton" 
                                          BorderBrush="{TemplateBinding BorderBrush}" 
                                          Background="{TemplateBinding Background}" 
                                          Foreground="{TemplateBinding Foreground}"
                                          Grid.Column="2" IsChecked="{Binding Path=IsDropDownOpen,Mode=TwoWay,RelativeSource={RelativeSource TemplatedParent}}" Focusable="false">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border x:Name="Border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1.5" CornerRadius="4">
                                            <Grid HorizontalAlignment="Right" Width="20">
                                                <Path x:Name="Arrow" Fill="#00E5FF" Data="M 0 0 L 4 5 L 8 0 Z" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Grid>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="Border" Property="Background" Value="#2A2F3A"/>
                                                <Setter TargetName="Border" Property="BorderBrush" Value="White"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <ContentPresenter Name="ContentSite" IsHitTestVisible="False"  Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                              ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                              Margin="5,0,15,0" VerticalAlignment="Center" HorizontalAlignment="Center" />
                            <Popup Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}" AllowsTransparency="True"  Focusable="False" PopupAnimation="Slide">
                                <Grid Name="DropDown" SnapsToDevicePixels="True" MinWidth="{TemplateBinding ActualWidth}" MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <Border x:Name="DropDownBorder" Background="#000000" BorderThickness="1.5" BorderBrush="#00E5FF" CornerRadius="4"/>
                                    <ScrollViewer Margin="4,6,4,6" SnapsToDevicePixels="True">
                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained" />
                                    </ScrollViewer>
                                </Grid>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ComboBoxItem">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}" BorderThickness="0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#00E5FF"/>
                                <Setter Property="Foreground" Value="Black"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#CC00E5FF"/>
                                <Setter Property="Foreground" Value="Black"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 极致对比度日历 -->
        <Style x:Key="ExtremeCalendarDayButton" TargetType="CalendarDayButton">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="ExtraBold"/>
            <Setter Property="MinWidth" Value="32"/>
            <Setter Property="MinHeight" Value="24"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CalendarDayButton">
                        <Grid>
                            <Border x:Name="SelectedBackground" Background="#00E5FF" Visibility="Collapsed" CornerRadius="4"/>
                            <Border x:Name="HoverBackground" Background="#44FFFFFF" Visibility="Collapsed" CornerRadius="4"/>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="SelectedBackground" Property="Visibility" Value="Visible"/>
                                <Setter Property="Foreground" Value="Black"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="HoverBackground" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsInactive" Value="True">
                                <Setter Property="Foreground" Value="#88FFFFFF"/> <!-- 让非本月日期也清晰可见 -->
                            </Trigger>
                            <Trigger Property="IsToday" Value="True">
                                <Setter Property="BorderBrush" Value="#00E5FF"/>
                                <Setter Property="BorderThickness" Value="2"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ExtremeCalendarButton" TargetType="CalendarButton">
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="FontWeight" Value="Black"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="MinWidth" Value="55"/>
            <Setter Property="MinHeight" Value="28"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CalendarButton">
                        <Grid Margin="2">
                            <Border x:Name="Background" Background="{TemplateBinding Background}" CornerRadius="6"/>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Opacity="1"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Background" Property="Background" Value="#00E5FF"/>
                                <Setter Property="Foreground" Value="Black"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 极致对比度头部按钮模板：强制 #FFFFFF，无视一切状态 -->
        <ControlTemplate x:Key="HeaderButtonTemplate" TargetType="Button">
            <Border Background="Transparent">
                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" 
                                  TextElement.Foreground="#FFFFFF" Opacity="1"/>
            </Border>
        </ControlTemplate>

        <Style x:Key="ExtremeCalendarItem" TargetType="CalendarItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CalendarItem">
                        <ControlTemplate.Resources>
                            <!-- 星期标题：核能级纯白 -->
                            <DataTemplate x:Key="{x:Static CalendarItem.DayTitleTemplateResourceKey}">
                                <TextBlock Text="{Binding}" Foreground="#FFFFFF" FontWeight="Black" FontSize="12" 
                                           HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,4"
                                           Opacity="1"/>
                            </DataTemplate>
                        </ControlTemplate.Resources>
                        <Grid x:Name="PART_Root" Background="Transparent" Opacity="1" TextElement.Foreground="#FFFFFF">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            
                            <!-- 头部控制区 -->
                            <Grid x:Name="PART_Header" Grid.Row="0" Margin="0,4" Opacity="1" Background="Transparent">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                
                                <Button x:Name="PART_PreviousButton" Template="{StaticResource HeaderButtonTemplate}"
                                        Foreground="#00E5FF" Width="32" Height="32" Content="◀" FontSize="18"/>
                                
                                <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Center">
                                    <Button x:Name="PART_HeaderButton" Template="{StaticResource HeaderButtonTemplate}"
                                            Foreground="#FFFFFF" FontWeight="Black" FontSize="16" VerticalAlignment="Center"/>
                                    
                                    <!-- 返回日期视图按钮：仅在非日期模式显示 -->
                                    <Button x:Name="PART_BackButton" Click="OnReturnToMonthClick" Margin="10,0,0,0"
                                            Background="#00E5FF" Foreground="Black" Padding="8,2" Content="返回" 
                                            FontSize="12" FontWeight="Bold" VerticalAlignment="Center" Visibility="Collapsed">
                                        <Button.Resources>
                                            <Style TargetType="Border">
                                                <Setter Property="CornerRadius" Value="4"/>
                                            </Style>
                                        </Button.Resources>
                                    </Button>
                                </StackPanel>
                                
                                <Button x:Name="PART_NextButton" Grid.Column="2" Template="{StaticResource HeaderButtonTemplate}"
                                        Foreground="#00E5FF" Width="32" Height="32" Content="▶" FontSize="18"/>
                            </Grid>
                            
                            <!-- 日期展示区 (7x7) -->
                            <Grid x:Name="PART_MonthView" Grid.Row="1" Visibility="Visible" Opacity="1">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                            </Grid>
                            
                            <!-- 月份/年份展示区 (4列 x 3行) -->
                            <Grid x:Name="PART_YearView" Grid.Row="1" Visibility="Collapsed" Opacity="1">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                            </Grid>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <!-- 核心逻辑修正：通过绑定父级 Calendar 的 DisplayMode 切换视图 -->
                            <DataTrigger Binding="{Binding DisplayMode, RelativeSource={RelativeSource AncestorType={x:Type Calendar}}}" Value="Year">
                                <Setter TargetName="PART_MonthView" Property="Visibility" Value="Collapsed"/>
                                <Setter TargetName="PART_YearView" Property="Visibility" Value="Visible"/>
                                <Setter TargetName="PART_BackButton" Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding DisplayMode, RelativeSource={RelativeSource AncestorType={x:Type Calendar}}}" Value="Decade">
                                <Setter TargetName="PART_MonthView" Property="Visibility" Value="Collapsed"/>
                                <Setter TargetName="PART_YearView" Property="Visibility" Value="Visible"/>
                                <Setter TargetName="PART_BackButton" Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 显式定义 Calendar 样式，通过 CalendarItemStyle 注入 -->
        <Style TargetType="Calendar">
            <Setter Property="Background" Value="#000000"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#00E5FF"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="CalendarDayButtonStyle" Value="{StaticResource ExtremeCalendarDayButton}"/>
            <Setter Property="CalendarButtonStyle" Value="{StaticResource ExtremeCalendarButton}"/>
            <Setter Property="CalendarItemStyle" Value="{StaticResource ExtremeCalendarItem}"/>
        </Style>

    </UserControl.Resources>

    <Grid>
        <!-- 头部展示区 -->
        <ToggleButton x:Name="HeaderButton" Click="OnHeaderClick" 
                      IsChecked="{Binding IsOpen, ElementName=PopupElement, Mode=TwoWay}"
                      ToolTip="{Binding ToolTip, RelativeSource={RelativeSource AncestorType={x:Type local:DateTimePicker}}}"
                      Height="32" Background="#1A1C23" BorderBrush="#00E5FF" BorderThickness="2">
            <ToggleButton.Template>
                <ControlTemplate TargetType="ToggleButton">
                    <Border x:Name="Border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="4">
                        <Grid Margin="12,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{Binding SelectedDateTime, StringFormat='yyyy-MM-dd HH:mm:ss', RelativeSource={RelativeSource AncestorType={x:Type local:DateTimePicker}}}" 
                                       Foreground="White" VerticalAlignment="Center" FontSize="15" FontWeight="ExtraBold"/>
                            <ContentControl Grid.Column="1" Template="{StaticResource DropdownIconTemplate}" Foreground="#00E5FF" Margin="10,0,0,0"/>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="#2A2F3A"/>
                            <Setter TargetName="Border" Property="BorderBrush" Value="White"/>
                        </Trigger>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="#000000"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </ToggleButton.Template>
        </ToggleButton>

        <!-- 选择弹窗 -->
        <Popup x:Name="PopupElement" PlacementTarget="{Binding ElementName=HeaderButton}" 
               Placement="Bottom" StaysOpen="False" AllowsTransparency="True" PopupAnimation="Slide">
            <Border Background="#000000" BorderBrush="#00E5FF" BorderThickness="2" CornerRadius="6" Padding="8" Margin="0,5,0,0">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="30" ShadowDepth="8" Opacity="1.0" Color="Black"/>
                </Border.Effect>
                <StackPanel>
                    <!-- 日历部分 -->
                    <Calendar SelectedDate="{Binding PickerDate, Mode=TwoWay, RelativeSource={RelativeSource AncestorType={x:Type local:DateTimePicker}}}" Margin="0,0,0,5" HorizontalAlignment="Center"/>
                    
                    <Separator Background="#444444" Margin="0,3"/>
                    
                    <!-- 时间选择部分 -->
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,5">
                        <TextBlock Text="时间选择" Foreground="#00E5FF" FontWeight="ExtraBold" FontSize="12" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        
                        <ComboBox ItemsSource="{Binding Hours, RelativeSource={RelativeSource AncestorType={x:Type local:DateTimePicker}}}" SelectedItem="{Binding SelectedHour, RelativeSource={RelativeSource AncestorType={x:Type local:DateTimePicker}}}" Style="{StaticResource TimeSelectorComboBox}"/>
                        <TextBlock Text=":" Foreground="White" FontWeight="ExtraBold" FontSize="14" VerticalAlignment="Center" Margin="5,0"/>
                        <ComboBox ItemsSource="{Binding Minutes, RelativeSource={RelativeSource AncestorType={x:Type local:DateTimePicker}}}" SelectedItem="{Binding SelectedMinute, RelativeSource={RelativeSource AncestorType={x:Type local:DateTimePicker}}}" Style="{StaticResource TimeSelectorComboBox}"/>
                        <TextBlock Text=":" Foreground="White" FontWeight="ExtraBold" FontSize="14" VerticalAlignment="Center" Margin="5,0"/>
                        <ComboBox ItemsSource="{Binding Seconds, RelativeSource={RelativeSource AncestorType={x:Type local:DateTimePicker}}}" SelectedItem="{Binding SelectedSecond, RelativeSource={RelativeSource AncestorType={x:Type local:DateTimePicker}}}" Style="{StaticResource TimeSelectorComboBox}"/>
                    </StackPanel>

                    <!-- 按钮部分 -->
                    <Grid Margin="0,5,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Button Content="设为现在" Click="OnNowClick" Margin="0,0,5,0"
                                Style="{StaticResource ActionButton}" Height="26" Background="#37474F" BorderThickness="0" FontWeight="ExtraBold" FontSize="11"/>
                        <Button Grid.Column="1" Content="确  定" Click="OnConfirmClick" Margin="5,0,5,0"
                                Style="{StaticResource ActionButton}" Height="26" Background="#0D47A1" BorderThickness="0" FontWeight="ExtraBold" FontSize="11"/>
                        <Button Grid.Column="2" Content="取  消" Click="OnCancelClick" Margin="5,0,0,0"
                                Style="{StaticResource ActionButton}" Height="26" Background="#B71C1C" BorderThickness="0" FontWeight="ExtraBold" FontSize="11"/>
                    </Grid>
                    <!-- 提示文字 -->
                    <TextBlock Text="{Binding ToolTip, RelativeSource={RelativeSource AncestorType={x:Type local:DateTimePicker}}}" 
                               Foreground="#FFA726" FontSize="10" TextWrapping="Wrap" Margin="0,5,0,0" Opacity="0.9"
                               HorizontalAlignment="Center" TextAlignment="Center"/>
                </StackPanel>
            </Border>
        </Popup>
    </Grid>
</UserControl>
